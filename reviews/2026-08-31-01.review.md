# Review 2026-08-31-01 — since tag 0.31.4

**Compile:** pyro clean (Scene, Scene_Manager).  
**Scope:** `0.31.4` (`ecbb8f1`)..HEAD. PublicAPI (`d197679`) and `0050_sexlab_activity.prompt` left as HEAD (author).

## Applied

| ID | Change |
|----|--------|
| **H1** | `GetSceneByThread`: keep/rebind when `bound == None` or `bound.tid == thread.tid` without requiring `IsActive()`. Setup still `STATUS_INACTIVE` until it finishes; `EnsureSceneForThread` must not `Release()` that slot. Release only on tid mismatch. |
| **L2** | `Scene.Setup`: drop pre-`PickNonVictimInitiator` initiator Trace; keep the post-pick Trace. |

## Skipped (author: leave as HEAD)

| ID | Why |
|----|------|
| **H2** | `Papyrus_WebUI.cpp` PublicAPI include → `extern` (`d197679`) |
| **M1** | 0050 consecutive-vocalization / word-cap |
| **L1** | 0050 typos |

## Deferred (still open)

- H6-arousal; M8-legacy orgasm_expected walk; M9–M16 UX; S10b Trace gating
- M8-notice schema; M9-params; L3 none category

## Smoke tests

1. DOM / external SexLab thread: first `HookAnimationStart` / `HookStageStart` during Scene.Setup does not Release the slot; `GetStageDescription` does not log `thread is None`.
2. Consensual scene with a victim: `"X initiates:"` uses a non-victim; Setup log shows initiator after pick only.
3. `threads.json` / `sexlab_get_threads` still populated for that thread after first stage.
