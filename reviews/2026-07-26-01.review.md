# Review 2026-07-26-01 — Apply pass (Q answers + ungated High/Medium)

**Compile:** pyro clean (Actions, Scene, Scene_Creator, Scene_Manager).  
**Scope:** Author Q1–Q5 answers + ungated findings from `review-execution-plan.xml`.

## Decisions

| Q | Decision | Action |
|---|----------|--------|
| Q1 | Remove `sexlab_none_rape.yaml` | Deleted; regenerated `actions_index.json` (33 actions) |
| Q2 | Leave arousal for future | H6 deferred |
| Q3 | Refuse when generic busy (no CK pool expand) | `GetThreadActive` gate + always `UnsetThread_scene` on Release |
| Q4 | Tentacles flavor in `GetIsOrgasming` | Flavor on orgasming actor only; removed AnimationEnd force-all loop |
| Q5 | `CreateCreator` → None on Setup fail; gate callers | `Setup` returns Bool; all call sites gated |

## Applied

| ID | Change |
|----|--------|
| **H1 / M1** | `Change_Outfit`: accept `dresses`/`undresses`/`dress`/`undress`; undress success only if strip returns items; unknown how no-op |
| **H2** | `UpdateActor`: assign `obj` before `notice_level` write; guard `obj > 0` |
| **H3** | Generic refuse if already `GetThreadActive`; Release always unsets `thread_scene[tid]` |
| **H4 / H5** | `0050_sexlab_activity.prompt`: removed UUID/speaker dumps; set `speaker.notice_level=2` only on match (no multi-thread clobber) |
| **H7** | Deleted `sexlab_none_rape.yaml` + label; index regenerated |
| **M2** | Refused intents: sexual activities / physically comforting / showing affection |
| **M3 / L1** | Tentacles flavor + "again" uses post-update `GetTotalOrgasms` |
| **M4 / Q5** | Creator.Setup → Bool; CreateCreator None on fail; CreateSceneWithoutCreator / GetSceneByThread gated |
| **M6** | Fixed-role assault/punish-rape `direction` static `""` |

## Deferred

- **H6 / Q2** — arousal population / 0550 gates (future)
- Prior backlog M8 / M9–M16 / S10b; M5 kissing setting; M7 Dom speaking_modifiers; M8 notice_level schema; M9 category PARAMS; L2 Stages Trace; L3 none category

## Smoke tests

1. Outfit: `dresses` restores; `undresses` strips; already-nude undress silent.
2. 11th concurrent SexLab thread: allocate fails cleanly (log refuse), does not corrupt active generic.
3. Tentacles orgasm: flavor only on actors that actually orgasm.
4. Refused sex/comfort/nonsexual: no "refused to allow refused".
5. CreateCreator with empty actors / broken links → None; Menu/Action abort without Lock.
