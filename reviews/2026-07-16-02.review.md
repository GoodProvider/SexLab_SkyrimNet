# SkyrimNet_SexLab Fix Pass — 2026-07-16 (02)

Follow-up review scoped to `Scripts/Source` only (per user request), cold-started from [review-guide.xml](../review-guide.xml) and [review-checkpoint.xml](../review-checkpoint.xml). The prior 2026-07-16 (01) pass (H-A..H-D, M-A..M-E, L-A..L-C) was confirmed applied and not re-reported. Findings in [review-execution-plan.xml](../review-execution-plan.xml). Excluded per guide/facts: compile errors, case mismatches, `PagedActors`, `z-*`, `Creatures.psc`, WebUI stub, `Trace("---...")`, deferred backlog (M7-M16, S10b, S11).

**Compile:** `pyro` / `skyrimse.ppj` — `SkyrimNet_SexLab_Menu` recompiled, 1 succeeded, 0 failed (exit 0).

All four findings are in [SkyrimNet_SexLab_Menu.psc](../Scripts/Source/SkyrimNet_SexLab_Menu.psc).

---

## High — Applied

| ID | Lines | Fix |
|----|-------|-----|
| **N-1** | `MutliTarget_Menu_Selection` ~440-457 | Out-of-bounds write. When the selection list was full (`next == selected.length`), the render loop stopped refreshing `indexes[]` for unselected rows, leaving a stale `-1`. Clicking such a row failed the add condition (`next < selected.length`) and fell into the remove `else` with `j = indexes[index] = -1`, doing `selected[-1] = selected[0]` (OOB) and a wrong `next -= 1`. Restructured so remove runs only when `indexes[index] != -1`, add only when `next < selected.length`, else no-op. (Prior M-D fix guarded only the add branch.) |

## Medium — Applied

| ID | Lines | Fix |
|----|-------|-----|
| **N-2** | punish branch ~138-142 | `SkyMessage.ShowArray(..., getIndex=false)` returns `""` on ESC/cancel; nothing checked it, so cancelling still called `StartScene_Nonconsensual_Two("punishing", ...)` with `method=""`. Added `if method == "" return` guard. |
| **N-3** | affection branch ~161-165 | Same pattern: cancel returned `""` and still called `StartScene_Consensual_Two("showing affection", ...)`. Added `if method == "" return` guard. |

## Low — Applied

| ID | Line | Fix |
|----|------|-----|
| **N-4** | setting picker ~434 | Off-by-one: `if 0 < idx && idx < setting_names.length` excluded index 0, so the first scene-setting `.json` was never selectable. Changed to `0 <= idx`. |

---

## Non-issues confirmed this pass
- Clothing "how" sub-menu cancel already guarded (`button < 0`, line ~205).
- H-B OStim `-1` collision already guarded (`sexlab_ostim != -1`, line ~123).
- Creator pool `Release()` balanced across all `CreateCreator` call sites (Action_Start, Menu multitarget, Creator.StartScene, GetSceneByThread, CreateSceneWithoutCreator).
- DOM `HandleOrgasmDenied` / `Orgasm_Desired` retain `slave.mind != None` guards; Interface stub and real Handler_DOM remain API-compatible.
- Scene / Scene_Manager / Scene_Creator / Utilities / Stages / Actions / Main / MCM / Interfaces / PR aliases: no new findings.
