# SkyrimNet_SexLab Fix Pass — 2026-07-18 (01)

Action YAML practice + parameter audit against [WORKFLOW_ACTIONS.md](https://github.com/MinLL/SkyrimNet-GamePlugin/blob/main/docs/modding/WORKFLOW_ACTIONS.md) and current `SkyrimNet_SexLab_Actions.psc` signatures. Plan: fix_action_yamls. No Papyrus signature changes required (Victim wrappers already existed).

**Scope:** all 29 files under `SKSE/Plugins/SkyrimNet/config/actions/` (+ `KNOWLEDGEBASE.md` practice note).

**Compile:** `pyro` / `skyrimse.ppj` — clean (user-confirmed). YAML-only pass; positional mappings revalidated vs Actions.psc.

---

## Applied

| ID | Area | Fix |
|----|------|-----|
| **H1** | `sexlab_sex_rape_target.yaml` | `StartScene_Nonconsensual_Two_TargetVictim`; dropped obsolete `speaking_victim`. |
| **H2** | `sexlab_sex_rape_by_target.yaml` | `StartScene_Nonconsensual_Two_SpeakerVictim`; dropped `speaking_victim`. |
| **H3** | `sexlab_punish_rape_target.yaml` | Same as H1 (TargetVictim); removed `victim` + `speaking_victim` arity mismatch. |
| **H4** | `sexlab_punish_rape_target_by_target.yaml` | Same as H2 (SpeakerVictim); removed orphan params. |
| **H5** | `sexlab_none_rape.yaml` | Deleted `StartScene_Nonconsensual_Two_Victim` → `StartScene_Nonconsensual_Two`; reordered `victim` after `target`. |
| **M1** | `sexlab_sex_refused.yaml` | Renamed `SexLab_Sexual_Refused` (was colliding with nonsexual refused); static `intent` → `value: refused`. |
| **M2** | `sexlab_sex_masturbation_raping.yaml` | Renamed `SexLab_Masturbation_Forced`; intent `sexual assault`. |
| **M3** | punish / giving / sex descriptions | `type` → `method` mapping names; `[type]` → `[method]` placeholders where Papyrus slot is `method`. |
| **M4** | `sexlab_punish_spanking_victim_nude.yaml` | static `method`; dynamic `direction` uses `description:` (not `value:`). |
| **L1** | category parents (`sexlab_*_.yaml`) | Valid PARAMS JSON; removed unused `description_`; direction vocab `giving\|getting`. |
| **L2** | stop / outfit actions | `speaker`; `stripper`/`stripped`/`how` names; eligibility `logicalOperator`/`required: true` where rules exist. |
| **L3** | `sexlab_nonsexual_general.yaml` | static `intent` `value: showing affection`. |

## Locked / practice

- Fixed-role nonconsensual two-actor actions use `TargetVictim` / `SpeakerVictim`; dynamic victim keeps `StartScene_Nonconsensual_Two` + explicit `victim`.
- Do not reintroduce `speaking_victim` in action YAMLs.
- Action `name` must be unique; `static` needs `value`, `dynamic` needs `description`.

## Superseded backlog

- **L-E** (informational): prior note that punish `direction` must keep `value:` is **obsolete** — victim role for fixed-role assault is now encoded by Victim wrappers; LLM-chosen direction (e.g. nude spanking) correctly uses `description:`.

## Deferred (unchanged)

- **M8**, remaining **M9–M16**, **S10b**, **L-D**.

## Smoke tests (in-game)

1. Sexual assault on Target / on Speaker — correct victim via TargetVictim / SpeakerVictim.
2. Punish rape Target / By_Target — same; setting `punish_pain_pleasure_rape`.
3. `SexLab_Start_Rape` (none) with dynamic victim — maps to `Nonconsensual_Two` correctly.
4. Sexual vs nonsexual refused — distinct action names in selector.
5. Forced vs consensual masturbation — distinct names; assault intent.
6. Outfit / stop — still dispatch with renamed params.
