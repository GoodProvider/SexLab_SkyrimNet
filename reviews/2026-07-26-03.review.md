# Review 2026-07-26-03 — Deferred M5 / M7 / L2

**Compile:** pyro clean (Scene_Creator, Scene_Manager, Stages).  
**Scope:** Next highest deferred backlog (skip arousal / legacy orgasm_expected / UX / S10b).

## Applied

| ID | Change |
|----|--------|
| **M5** | `Creator.Setup`: `_method == "kissing"` → `setting_name = "nonsexual_kissing"` before LoadSetting (LLM comfort/nonsexual general get `_kissing_` + kissing tags; matches Menu) |
| **M7** | `EnrichActorObjForJson`: empty `speaking_modifiers` JArray when missing (Dom-path prompt `contains`/`length` safe) |
| **L2** | Stages Trace calls use `(func, msg, true)` — GetStageDescription / GetAnim_Info / UpdateAnimInfo |

## Deferred (still open)

- H6-arousal; M8-legacy orgasm_expected walk; M9–M16 UX; S10b Trace gating
- M8-notice schema; M9-params; L3 none category

## Smoke tests

1. Comfort/nonsexual LLM action with method kissing → scene loads `nonsexual_kissing` (`_kissing_` speaking_modifiers).
2. Dom thread JSON actors include `"speaking_modifiers":[]`.
3. Stages parse/save notifications fire when Trace notification=true paths hit.
