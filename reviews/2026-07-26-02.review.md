# Review 2026-07-26-02 — Apply bug fixes (H1–H6, M1–M9)

**Compile:** pyro clean (Actions, Menu, Scene, Scene_Creator, Scene_Manager, Stages).  
**Scope:** Fresh review findings applied per locked author decisions.

## Locked decisions

| Topic | Decision |
|-------|----------|
| no_penis / sex2 | Speaker-mode split (fucking vs giving); not “drop dual-tagged methods” |
| M2 | sex3 giving `setting_name: ""` to match sex2 |
| H2 | SelectAnimations before NewThread; `model.Initialize` only on post-NewThread abort |
| M1 | Keep LLM `cuddle`; Papyrus → `cuddling` |
| M5 | Blank MultiTarget intent → `"sexual activities"` |
| M8 | LLM `how` = `dresses\|undresses` with real `[how]` |

## Applied

| ID | Change |
|----|--------|
| **H1** | Removed `hugging`→`hug` remap |
| **H2** | SelectAnimations before NewThread; Initialize on AddActor fail / StartThread None |
| **H3** | Hook `AnimationEnd` uses `create_if_missing=False` |
| **H4** | Dom Combined fallback: totals&gt;0 → `name+" is orgasming. "`; else denied |
| **H5** | EditDescriptions ESC/`button&lt;0` exit before indexing |
| **H6** | SetOrgasmExpected ESC returns (save if changed); Go Back still opens editor |
| **M1** | `cuddle`→`cuddling` remap in Actions |
| **M2** | `sexlab_sex3_sex_giving.yaml` setting_name `""` |
| **M3** | Outfit YAMLs → `OStimActorCountFaction` |
| **M4** | `Scene.Setup`→Bool; `CreateSceneByCreator` Release+None on fail |
| **M5** | MultiTarget blank intent → `"sexual activities"` |
| **M6** | `GetAnim_Info` releases per-file roots after merge |
| **M7** | `UpdateAnimInfo` aborts save on parse fail (no `{}` wipe) |
| **M8** | Outfit helper `[style] [how]`; YAML `dresses\|undresses` |
| **M9** | `0550` speaker.active = UUID-in-thread (match 0050) |

## Deferred (unchanged)

- H6-arousal; kissing→nonsexual_kissing; Dom speaking_modifiers; notice_level schema; category PARAMS
- L2 Trace arg order; L3 none category; S10b; M9–M16 UX

## Smoke tests

1. Affection `hugging` / comfort `cuddle` pick tagged anims (not random untagged).
2. Cancel animation picker before start: no 60s Making slot lock.
3. Action_Stop mid-scene: single AnimationEnd / no duplicate afterglow.
4. Dom Combined race: empty stash + totals&gt;0 still contains `" is orgasming."`.
5. EditDescriptions / SetOrgasmExpected ESC exits cleanly.
6. Sex3 giving uses empty setting (same as sex2 giving).
7. Outfit action: LLM `dresses`/`undresses` via `[how]`; eligibility uses ActorCount faction.
