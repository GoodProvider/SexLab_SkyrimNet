# SkyrimNet_SexLab Fix Pass — 2026-07-08

Applied all scoped findings from [review-execution-plan.xml](../review-execution-plan.xml). Deferred M7–M16 per plan.

**Compile:** `papyrus-compiler` — 19 files, zero errors.

---

## Critical / High — Applied

| ID | File | Fix |
|----|------|-----|
| **H4** | `SkyrimNet_SexLab_Main.psc` | SexLab gate: abort when `GetModByName == 255` (missing), not when loaded |
| **H1** | `SkyrimNet_SexLab_Scene_Creator.psc` | `CreateSceneByCreator` before `Release()` so style survives handoff |
| **M6** | `SkyrimNet_SexLab_Scene_Creator.psc` | `SetStyle` delegates to `parent.SetStyle` for normalization |
| **H2** | `SkyrimNet_SexLab_Scene_Manager.psc` | Cache thread before `AnimationEnd`; `StopThread(cachedThread)` |
| **NEW-1** | `SkyrimNet_SexLab_Scene_Manager.psc` | `AnimationEnd`+`Release` always; consent dialog only when `has_player` |
| **NEW-2** | `SkyrimNet_SexLab_Scene_Manager.psc` | Validate `target == None` after Actor cast |
| **H3** | `SkyrimNet_SexLab_Scene_Manager.psc` | Creator null-check; `Release()` on lock failure |
| **H5** | `SkyrimNet_SexLab_Stages.psc` | v2: `JArray.setStr(actors_obj,…)`; `JMap.setObj(obj,"actors",…)` |
| **H6** | `SkyrimNet_SexLab_Menu.psc` | Remove `+"es"` on outfit string |
| **H6b** | `SkyrimNet_SexLab_Actions.psc` | Dress branch on `how == "dress"` after normalization |
| **H7** | `SkyrimNet_SexLab_Utilities.psc` | Full narration pipeline per [review-execution-h7-spec.md](../review-execution-h7-spec.md) |
| **H7b** | `SkyrimNet_SexLab_Scene.psc` | First `StageStart` always prepends intent; empty desc → `ContinueActivity(..., True)` |
| **H8** | `SkyrimNet_SexLab_Scene.psc` | `SetActor` writes `is_dom_slave` from `handler_dom.IsDOMSlave` |
| **H10** | `SkyrimNet_SexLab_Menu.psc` | DOM slave menu path via `IsDOMSlave` + `Target_Menu_Selection` |

## Medium — Applied

| ID | File | Fix |
|----|------|-----|
| **M1** | `SkyrimNet_SexLab_Scene_Manager.psc` | `UnRegisterForModEvent` before Action Start/Stop register |
| **M2** | `SkyrimNet_SexLab_Scene_Manager.psc` | Generic fallback sets `thread_scene[tid]` |
| **M3** | `SkyrimNet_SexLab_Scene_Manager.psc` | `GetThreadsJson` releases root `obj` only |
| **M4** | `SkyrimNet_SexLab_Menu.psc` | `MultiTarget_Menu_Selection`: creator null + `LockAllActorLock` |
| **M5** | `SkyrimNet_SexLab_Decorators.psc` | `Intent` manager None-guard |
| **Y1–Y3** | Action YAMLs | `description:` on dynamic `participate`; `customCategory: sexlab_nonsexual` |
| **P2–P8** | Prompts | Decorator/global ID fixes, typo fixes, `sexlab_outfit_options` refactor |

## Papyrus JSON / Decorator additions

- **`GetObj`** exports `names`, `victims` (string arrays), `location`
- **`SetActor`/`UpdateActor`** set `notice_level` = `"active"` when scene active
- **`sexlab_activities`** decorator registered → `Activities(Actor)` returns `{"activity":…}`

## Deferred (unchanged)

M7–M16, Menu N1–N4 copy, Stages N5–N8 minor text.

## Smoke tests (in-game)

1. Mod loads with SexLab present
2. LLM scene with `style: forcefully` preserves style
3. Stop scene — SexLab thread ends
4. v2 stage descriptions substitute `{{sl.actors.N}}`
5. Menu dress path redresses actor
6. `sexlab_activities` returns description in stop prompt
7. Bio prompt `actor_having_sex` / `actor_rape_victim` during active sex
8. NPC-only scene stop releases pool slot
