# SkyrimNet_SexLab Fix Pass — 2026-07-16 (03)

Deferred-backlog fix pass driven by [review-guide.xml](../review-guide.xml) and [review-checkpoint.xml](../review-checkpoint.xml). Scope: 6 backlog items (M7, hide_help, victim_faction, trace_labels, dom_pr, S11). S10b stays deferred. L-E was dropped by design (see below). Line numbers include commented lines.

**Compile:** `pyro` / `skyrimse.ppj` — 6 scripts recompiled, 0 failed (exit 0).

---

## Applied

| ID | File | Fix |
|----|------|-----|
| **M7** | `SkyrimNet_SexLab_Stages.psc` | `GetAnim_Info` now `JValue.retain`s the object it returns; all 5 callers (`GetStageDescription`, `GetOrgasmExpected`, `SetOrgasmExpected`, `GetHasDescriptionOrgasmExpected`, `EditDescriptions`) `JValue.release` it after reading. Rebuilt fresh each call (animation AND stage can change between calls), so no caching — just deterministic free instead of relying on JContainers auto-GC churn. |
| **hide_help** | `SkyrimNet_SexLab_Stages.psc` | `EditDescriptions` gates the "You may enter a description..." help + example behind `if !hide_help`; when hidden it shows a minimal `Stage N: (no description)` line. The MCM `SexEditHelpToggle` was already wired to `stages.hide_help`. |
| **victim_faction** | `SkyrimNet_SexLab_Scene.psc` | New retained JContainers `JArray` `victim_faction_forms` tracks actors granted `SkyrimNet_SexLab_Faction_Victim`. New `ReconcileVictimFactions()` adds the faction to current victims, removes it from current non-victims, and clears it from tracked actors no longer a current victim (departed / reshuffled). Called from `Setup` (replacing the inline loop) and `AlignActors`. `Release` now also clears the faction from every tracked form and empties the tracker. Honors the "do not cache a parallel `Actor[]`" rule by using a JArray of forms. |
| **trace_labels** | `SkyrimNet_SexLab_Scene_Manager.psc`, `SkyrimNet_SexLab_Scene_Creator.psc` | Corrected mislabeled `Trace(...)` function tags: Manager `CreateSceneWithoutCreator` (was `"CreateSceneByThread"`), `GetSceneByThread` message (was "CreateSceneWithoutCreator returned None" -> "CreateSceneByCreator returned None"), `OrgasmIndividual` event (was `"OrgasmCombined"`); Creator `StartScene` (was `"Start"`), `SetVictims` (was `"SexVictims"`). Log-only. |
| **dom_pr** | `SkyrimNet_SexLab_Handler_DOM_PR.psc` | Removed the dead commented `;    handler.Setup()` in `OnPlayerLoadGame`; replaced with a comment noting the alias is an intentional no-op (DOM init owned by `Main.Setup`). Behavior unchanged. |
| **S11** | `SkyrimNet_SexLab_Scene.psc` | `GetLocation` furniture branch now maps each tag to a natural, preposition-led phrase via a parallel `on_furniture_phrase[]` (e.g. `Table` -> "on a table", `WoodenPony` -> "on a wooden pony", `Cage` -> "in a cage"), matching the `!found` fallback style, instead of emitting the raw tag. |

## Dropped (intentional)

- **L-E** — punish `direction` param stays `type: dynamic`. `giving|getting` decides whether the speaker or the target is the victim, so it must remain LLM-chosen; the `value:`/`description:` cosmetic change was skipped to avoid risking that dispatch. Punish YAMLs left untouched.

## Deferred (unchanged)

- **S10b** — always-on `Trace("---...")` scaffolding -> `debug_mode`-gated `DbgMsg` (Scene.psc).
- **M8** — `orgasm_expected` per-animation vs per-stage description-walk semantics (needs author confirmation).
- **Orgasm editor polish** — no concrete acceptance criteria yet.
- **L-D** — registered-but-unconsumed decorators (`_intent`, `_distance`/`_los`), informational.

## Smoke tests (in-game)

1. Long multi-stage scene — stage descriptions/orgasm-expected still resolve; no JContainers errors (M7).
2. MCM "Hide Edit Stage Description Help" ON — the stage-description editor shows the minimal line, not the help/example (hide_help).
3. Scene where a victim leaves mid-run (position swap / reshuffle) — departed actor no longer in `SkyrimNet_SexLab_Faction_Victim`; current victims still tagged; scene end clears all (victim_faction).
4. Furniture animation (table / wooden pony / cage / X-cross) — `_location` reads as a natural phrase (S11).
