# SkyrimNet_SexLab Fix Pass — 2026-07-17 (01)

Scoped review of files newer than [review-checkpoint.xml](../review-checkpoint.xml) (`LastWriteTime` 2026-07-16 10:06), then fix → compile → checkpoint.

**Scope:** `SkyrimNet_SexLab_Scene.psc`, `SkyrimNet_SexLab_Stages.psc`, `sexlab_punish_rape_target_by_target.yaml` (plus arity check on Actions / other punish YAML — no content change).

**Compile:** `pyro` / `skyrimse.ppj` — Scene + Stages recompiled, 0 failed (exit 0).

---

## Applied

| ID | File | Fix |
|----|------|-----|
| **H1** | `Scene.psc` | First StageStart: after initiator overwrite, `+= orgasm_narration` so `" is orgasming."` is kept. |
| **H2** | `Scene.psc` | `AnimationStart`: if already `STATUS_ACTIVE`, do not force `STATUS_SETUP` or clear orgasm stash; only first-start clears slots + flag. |
| **H3** | `Stages.psc` | `EditDescriptions`: `if anim_info != 0` then `JValue.release` (was `< 0`, never released after retain). |
| **M1** | `Scene.psc` | Only set `description_last` when `desc != ""` (unchanged path no longer wipes). |
| **M2** | `Scene.psc` | `AnimationEnd`: if `orgasm_narration != ""`, always append and DN (player) / Optional `"orgasm"` (NPC). |
| **M3** | `Scene.psc` | `OrgasmIndividual`: `" is orgasming. "` / `" is orgasming. again. "` for prompt gate. |
| **M4** | `Stages.psc` | **Locked:** one style `DirectNarration` on live Scene `EditDescriptions` button only; exit duplicate removed; Creator/`SetStyleDialog` stay silent. |
| **M5** | `sexlab_punish_rape_target_by_target.yaml` | `description` → `on [speaker]` (matches `speaking_victim: true`). |
| **L1** | `Scene.psc` | Dead preserve branch removed; scene-change assign then `+= orgasm_narration`. |
| **L2** | `Scene.psc` | Removed unused locals in StageStart else / OrgasmCombined. |

## Locked (new)

- **M4** — Style DN: one on live Scene only; none on Scene_Creator.

## Deferred (unchanged)

- **M8**, remaining **M9–M16** (orgasm editor / OStim affection), **S10b**, **L-D**.

## Smoke tests (in-game)

1. Combined orgasm + stage description change — DN has `" is orgasming."` and `"Scene changes to"`.
2. First StageStart with initiator + pending orgasm — initiator line includes orgasm clause.
3. Mid-scene animation change — status stays ACTIVE; pending Combined messages still flush.
4. EditDescriptions style change — exactly one DN; Creator style pick silent.
5. EditDescriptions loop — no JContainers retain leak (H3).
6. NPC AnimationEnd with Combined messages — Optional orgasm DN.
7. SeparateOrgasms individual orgasm — DN contains `" is orgasming."`.
8. Punish rape by_target — action description says assault on speaker.
