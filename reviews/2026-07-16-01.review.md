# SkyrimNet_SexLab Fix Pass — 2026-07-16

Fresh comprehensive review of all `Scripts/Source` (plus action YAMLs and prompt schema cross-check) per [review-guide.xml](../review-guide.xml), cold-started from [review-checkpoint.xml](../review-checkpoint.xml). Findings in [review-execution-plan.xml](../review-execution-plan.xml). Already-applied fixes (H1–H10, M1–M6, S1–S10) were not re-reported. Excluded per guide/facts: compile errors, case mismatches, `PagedActors`, `z-*`, `Creature.psc`, WebUI stub.

**Compile:** `pyro` / `skyrimse.ppj` — 5 changed scripts recompiled, 0 failed (exit 0).

---

## Critical / High — Applied

| ID | File | Fix |
|----|------|-----|
| **H-A** | `SkyrimNet_SexLab_Scene_Manager.psc` | `GetSceneByThread` + `CreateSceneWithoutCreator` now `creator.Release()` after `CreateSceneByCreator` (was leaking fixed-pool creator slots on every unmapped/external thread until descriptions stopped) |
| **H-B** | `SkyrimNet_SexLab_Menu.psc` | `Target_Menu_Selection` OStim branch guarded `sexlab_ostim != -1 && button == sexlab_ostim` so ESC/cancel (-1) no longer silently toggles the sex framework when OStimNet is absent |
| **H-C** | `prompts/helpers/sexlab_none_stop.prompt` | Read underscored key `_activity` (with non-empty guard) to match the `Activities` decorator; stop narration now uses the real activity instead of always defaulting |
| **H-D** | `prompts/helpers/sexlab_none_change_outfit.prompt` | Read `_options` to match the `Outfit_Options` decorator; change-outfit verb no longer renders blank |

## Medium — Applied

| ID | File | Fix |
|----|------|-----|
| **M-A** | `SkyrimNet_SexLab_Scene.psc` | `SetPosition` `_speaking_modifiers`: separate compacting write index (`w`) so empty split segments (e.g. `"a,,b"`) can't cause out-of-range/misaligned `JArray.setStr` |
| **M-B** | `SkyrimNet_SexLab_Scene.psc` | `AlignActors` relinks every `actors_objs` slot after the update loop so a position-count change no longer leaves null `_actors` entries for unchanged actors |
| **M-C** | `SkyrimNet_SexLab_Menu.psc` | Clothing "how" sub-menu returns on `button < 0` instead of performing the dress/undress on cancel |
| **M-D** | `SkyrimNet_SexLab_Menu.psc` | Multitarget add guarded with `next < selected.length` to prevent `selected[5]` overflow with 6+ eligible actors |
| **M-E** | `SkyrimNet_SexLab_Handler_DOM.psc` | `HandleOrgasmDenied` / `Orgasm_Desired` add `slave.mind != None` guards (defensive; DOM can leave `mind` unpopulated on cloning/mid-setup) |

## Low / Polish — Applied

| ID | File | Fix |
|----|------|-----|
| **L-A** | `SkyrimNet_SexLab_Menu.psc` | Removed dead `elseif intent == "affection"` branch (menu only offers "showing affection") |
| **L-B** | `SkyrimNet_SexLab_Menu.psc` | Affection button now shows a notification instead of being a silent no-op when OStim is the active framework |
| **L-C** | `SkyrimNet_SexLab_Utilities.psc` | `DirectNarration` / `DirectNarration_Optional` add `main == None` guard before dereferencing `main.*` |

## Not a bug (verified)

- Action YAMLs: all 23 executable YAMLs match their `Actions.psc` targets on parameter count/order/type. Only cosmetic `type: dynamic` + `value:` authoring noted (L-E, informational).
- DOM Interface stub is a safe no-op/passthrough; `Main.Setup` guarantees `handler_dom` non-None.
- `H9` Manager Dom orgasm early-return — external DOM calls `DOMSlave_Orgasmed`.

## Informational (no change)

- **L-D** — `Intent` (`_intent`) and `Player_LOS_Distance` (`_distance`/`_los`) decorators are registered but no prompt consumes them.
- **L-E** — `punish_*` YAMLs use `type: dynamic` with `value:` (cosmetic).

## Deferred (unchanged)

M7–M16, S10b (Trace→DbgMsg), S11 (furniture wording), Handler_DOM_PR commented `Setup()`.

## Smoke tests (in-game)

1. Repeated external/NPC-only threads — creator pool not exhausted; scene descriptions keep generating (H-A)
2. With OStimNet absent, cancel the target menu — framework NOT toggled (H-B)
3. Stop prompt narrates the real activity; change-outfit prompt shows the verb (H-C/H-D)
4. Scene with a leading/embedded empty speaking-modifier token — no error (M-A)
5. Scene where position count changes mid-scene — `_actors` JSON stays populated (M-B)
6. Cancel the clothing "how" sub-menu — no outfit change performed (M-C)
7. Multitarget menu with 6+ eligible actors — no array error (M-D)
