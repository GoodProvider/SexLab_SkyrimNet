**High**
- **File**: [Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc](Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc#L68-L95)  
  **Problem**: `GetSceneByThreadId` contains multiple issues: misspelled properties (`lenth`), incorrect resizing logic, uses undefined identifiers (`thread.positions`, `threadID`) and unsafe casting.  
  **Fix**: Rewrite the function to (1) validate `tid`, (2) resize `thread_scene` safely (preserve entries), (3) read the thread by ID using the correct API (`ThreadSlots.Threads[tid]` or SexLab controller lookup), and (4) store the scene at `thread_scene[tid]` after correct casting.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc](Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc#L144-L162)  
  **Problem**: `GetSceneNext` is declared `SkyrimnNet_SexLab_Scene` (typo) and contains undefined variables and logic (uses `actors`/`akActor` without declaration, writes locks with undefined identifiers, returns inconsistent scene selection).  
  **Fix**: Correct the return type name to `SkyrimNet_SexLab_Scene`, use clearly scoped loop variables (e.g., `i`), accept the `actors` parameter and iterate over it to set per-actor locks via `StorageUtil.SetIntValue(actor, key, sid)`. Make selection logic explicit: find an inactive scene, else allocate/return `scene_generic`.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc](Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc#L170-L176)  
  **Problem**: `ReleaseScene` bounds-checks incorrectly (`scene.tid < thread_scene`) and uses undefined `tid` when clearing `thread_scene`.  
  **Fix**: Replace with `if 0 <= scene.tid && scene.tid < thread_scene.length` then `thread_scene[scene.tid] = None`.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc](Scripts/Source/SkyrimNet_SexLab_Scene_Manager.psc#L222-L231)  
  **Problem**: Calls use both `SceneRelease(scene)` and `ReleaseScene(scene)` inconsistently; function name mismatch will cause runtime errors.  
  **Fix**: Standardize on one name (recommend `ReleaseScene`) and update all call sites accordingly.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Scene.psc](Scripts/Source/SkyrimNet_SexLab_Scene.psc#L80-L92)  
  **Problem**: `Setup` uses undefined variables (`count`), inconsistent arrays (`number_orgasm` vs `number_orgasms`), and calls to utility helpers with likely-misspelled names (`PapyrusUtil.ActorArray` usage and `SkyrimNetAPI.JoinStrings(actor_names)` expecting strings).  
  **Fix**: Initialize variables correctly (use `_actors.length` for counts), keep naming consistent (`number_orgasms` vs `number_orgasms` throughout), and verify helper method signatures. Build `actors_names` from `_actors` then call `SkyrimNetAPI.JoinStrings(actors_names)`.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Scene.psc](Scripts/Source/SkyrimNet_SexLab_Scene.psc#L152-L221)  
  **Problem**: `StartThread` loop calls `thread.addActor(actors)` (passing array rather than single actor) and then has logic that never increments or populates `victims`/`aggressors` correctly (missing index increments, string-builder errors).  
  **Fix**: Call `thread.AddActor(actors[i])` per actor, check return values, and implement victim/aggressor collection using indexed counters (increment `v_i` and `a_i`).

- **File**: [Scripts/Source/SkyrimNet_SexLab_Scene.psc](Scripts/Source/SkyrimNet_SexLab_Scene.psc#L248-L318)  
  **Problem**: `AnimationEnd` contains many typos: malformed `Trace` string concatenation, wrong symbol names (`num_orgasms` vs `number_orgasms`), `DirectNarration_optional` vs `DirectNarration_Optional`, and misspelled constants (`STYE_NORMALLY`).  
  **Fix**: Normalize symbol names, fix `Trace` concatenation, use `DirectNarration_Optional`, and set `style = STYLE_NORMALLY`.

**Medium**
- **File**: [Scripts/Source/SkyrimNet_SexLab_Actions.psc](Scripts/Source/SkyrimNet_SexLab_Actions.psc#L76-L90)  
  **Problem**: Multiple action wrappers pass lowercase `speaker`, `target`, `participate` identifiers to `SceneStart_Event` while parameters are `Speaker`, `Target` (case-sensitive in Papyrus).  
  **Fix**: Use the function parameter names exactly (`SceneStart_Event(Speaker, Target, Victim, ...)`) or rename parameters consistently. Ensure optional parameters are passed in the correct order or by name if supported.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Stages.psc](Scripts/Source/SkyrimNet_SexLab_Stages.psc#L216-L220)  
  **Problem**: Calls `manager.GetSceneFromThread(thread)` — ensure `manager` property points to `SkyrimNet_SexLab_Scene_Manager` and that `thread` variable is in scope; otherwise this will error at runtime.  
  **Fix**: Verify `manager` property wiring in the Creation Kit and guard calls with `if manager != None` and `if thread != None`.

- **File**: [Scripts/Source/SkyrimNet_SexLab_MCM.psc](Scripts/Source/SkyrimNet_SexLab_MCM.psc#L24-L36)  
  **Problem**: Several property accessor wrappers (e.g., `sexlab_ostim_player` getter) reference `skyrimnet_sexlab_ostim_player` and commented logic; inconsistent naming and case may break bindings.  
  **Fix**: Standardize property names and ensure `GlobalVariable` properties used in getters match exactly.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Utilities.psc](Scripts/Source/SkyrimNet_SexLab_Utilities.psc#L1-L20)  
  **Problem**: `JoinActors` signature uses `ACtor[]` capitalization error.  
  **Fix**: Change to `Actor[]` for consistency.

**Low**
- **File**: [Scripts/Source/SkyrimNet_SexLab_Decorators.psc](Scripts/Source/SkyrimNet_SexLab_Decorators.psc#L1-L40)  
  **Problem**: Large blocks of commented code remain; while harmless they make review harder and may be dead code.  
  **Fix**: Remove or move legacy code to `old_scripts/` and leave a concise reference comment.

- **File**: [Scripts/Source/SkyrimNet_SexLab_WebUI.psc](Scripts/Source/SkyrimNet_SexLab_WebUI.psc#L1-L5)  
  **Problem**: `Sex_Menu_Open` is defined empty; either implement or annotate as intentionally stubbed.  
  **Fix**: Add a TODO comment or implement the web UI hook.

- **File**: [Scripts/Source/SkyrimNet_SexLab_Creatures.psc](Scripts/Source/SkyrimNet_Sexlab_Creatures.psc#L1-L40)  
  **Problem**: Some JContainers usage appears correct but variable `races` is created via `JMap.object()` then treated as an integer; consider consistent naming and release semantics.  
  **Fix**: Add explicit type comment and ensure `JValue.retain/release` usage follows project conventions.

**Action items / next steps**
- I can produce a targeted patch for the highest-severity functions (`GetSceneByThreadId`, `GetSceneNext`, `ReleaseScene`, `StartThread`, `AnimationEnd`) if you want.  
- Do you want me to (A) open PR-style patches for the fixes, or (B) continue scanning for more subtle dead code (unused functions/exports) across the repo?

**Dead-code and duplicates (quick scan)**
- **Duplicate function**: `Orgasm_Custom` appears twice in `SkyrimNet_SexLab_Scene_Manager.psc` (lines ~542 and ~612). Problem: duplicate symbols will break compilation. Fix: remove or merge duplicates and keep a single implementation.
- **Empty / stubbed function**: `Sex_Menu_Open` in `SkyrimNet_SexLab_WebUI.psc` is empty. If not used, mark as TODO or remove. If needed, implement the WebUI hook.
- **Inconsistent function names / calls**: `ReleaseScene` vs `SceneRelease` both used; standardize to a single name (recommend `ReleaseScene`) and update all call sites.
- **Type/identifier typos**: multiple typos found that create dead code or runtime errors, e.g.: `SkyrimnNet_SexLab_Scene` (misspelled type), `ACtor`/`ACtor` capitalization, `STYE_NORMALLY`, `num_orgasm` vs `number_orgasms`, `Scehe` in `Trace`. Fix: normalize spelling and run a compile pass.
- **Invalid code snippets left in source**: several lines include explanatory signatures or malformed calls (e.g., `scene.addOrgasm(Actor akActor, String msg)` text inside code). Remove or correct to valid Papyrus calls.
- **Wrong parameter names / case-sensitivity**: many wrapper functions in `SkyrimNet_SexLab_Actions.psc` call `SceneStart_Event` with lowercase `speaker`/`target` while parameters are `Speaker`/`Target`. Papyrus is case-sensitive; fix parameter usage.

These are the highest-confidence dead-code / duplication issues. I can now (choose one):
- apply automated fixes for the critical items above and run a compile check, or
- continue a deeper automated scan to list every function that has zero references.
